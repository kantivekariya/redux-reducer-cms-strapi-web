{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "client": "mongo",
        "srv": true,
        "host": "cluster0-fm9rg.mongodb.net",
        "port": "${process.env.DATABASE_PORT || 27017}",
        "database": "cms_stripe",
        "username": "kantivekariya",
        "password": "kanti@albiorix"
      },
      "options": {}
    }
  }
}
